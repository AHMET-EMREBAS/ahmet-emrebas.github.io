properties:
  title:
    type: String
    inputType: text
    maxLength: 50
  content:
    type: String
    inputType: texteditor

relations:
  blog:
    type: ManyToOne
    target: blog
    inputType: find-one
    onDelete: CASCADE
