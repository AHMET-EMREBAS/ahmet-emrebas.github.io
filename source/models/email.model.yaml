properties:
  email:
    type: String
    inputType: text
    required: true
    unique: true
    email: true

relations:
  owner:
    type: ManyToOne
    target: user
    inputType: owner
    viewColumns:
      username:
        as: username
        type: String
