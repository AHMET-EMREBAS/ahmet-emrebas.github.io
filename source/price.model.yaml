properties:
  price:
    type: Number
    inputType: currency
    minimum: 0
    required: true

  cost:
    type: Number
    inputType: currency
    minimum: 0
    required: true

relations:
  product:
    type: ManyToOne
    target: product
    inputType: find-one
    eager: true
    onDelete: CASCADE
    onUpdate: CASCADE

  pricelevel:
    type: ManyToOne
    target: pricelevel
    inputType: find-one
    eager: true
    onDelete: CASCADE
    onUpdate: CASCADE
