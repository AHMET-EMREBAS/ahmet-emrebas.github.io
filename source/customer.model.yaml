properties:
  uuid:
    type: String
    inputType: uuid
    generated: uuid

relations:
  user:
    type: OneToOne
    target: user
    inputType: find-one

  pricelevel:
    type: ManyToOne
    target: pricelevel
    inputType: find-one
