# yaml-language-server: $schema=https://raw.githubusercontent.com/AHMET-EMREBAS/schemas/main/v1.0.0/resource.json
type: basic-resource
properties:
  name:
    type: string
    minLength: 0
    maxLength: 400
  category:
    type: many-to-one
    target: product

required:
  - name
