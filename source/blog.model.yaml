properties:
  title:
    type: String
    inputType: text
    maxLength: 50

relations:
  article:
    type: OneToMany
    target: article
    inputType: find-many
    nullable: true
    cascase:
      - insert
    eager: true

  owner:
    type: ManyToOne
    inputType: owner
    target: user
