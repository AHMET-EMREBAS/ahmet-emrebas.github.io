# rg

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build rg` to build the library.

## Running unit tests

Run `nx test rg` to execute the unit tests via [Jest](https://jestjs.io).
