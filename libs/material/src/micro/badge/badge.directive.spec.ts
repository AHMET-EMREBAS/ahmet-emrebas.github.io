import { BadgeDirective } from './badge.directive';

describe('BadgeDirective', () => {
  it('should create an instance', () => {
    const directive = new BadgeDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
