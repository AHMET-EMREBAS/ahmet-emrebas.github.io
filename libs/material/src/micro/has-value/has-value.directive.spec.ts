import { HasValueDirective } from './has-value.directive';

describe('HasValueDirective', () => {
  it('should create an instance', () => {
    const directive = new HasValueDirective();
    expect(directive).toBeTruthy();
  });
});
