import { ClassListDirective } from './class-list.directive';

describe('ClassListDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassListDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
