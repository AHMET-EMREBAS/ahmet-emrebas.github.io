import { LayoutToolbarDirective } from './layout-toolbar.directive';

describe('LayoutToolbarDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutToolbarDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
