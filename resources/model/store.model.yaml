properties:
  name:
    type: NameProperty

relations:
  priceLevel:
    target: PriceLevel
    type: ManyToOne
    views:
      name:
        as: priceLevel

required:
  - name

unique:
  - name

searchable:
  - name
