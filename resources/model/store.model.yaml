properties:
  name:
    type: string
    minLength: 2
    maxLength: 30

relations:
  priceLevel:
    target: PriceLevel
    type: ManyToOne
    views:
      name:
        as: priceLevel

required:
  - name
