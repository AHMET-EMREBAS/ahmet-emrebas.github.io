relations:
  resource:
    type: ManyToOne
    target: Resource
    join: true

  operation:
    type: ManyToOne
    target: Operation
    join: true

views:
  - target: Resource
    parent: Permission
    viewFields:
      name:
        type: string

  - target: Operation
    parent: Permission
    viewFields:
      name:
        type: string
